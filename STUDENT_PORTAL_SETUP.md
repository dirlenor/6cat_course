# ระบบนักเรียนและจัดการคอร์ส 6CAT

ระบบนี้ใช้ Supabase Auth + Database/RLS และใช้ YouTube แบบ Unlisted สำหรับวิดีโอ จึงไม่มีค่า API เพิ่มจาก OpenAI และเริ่มต้นได้ด้วยแพ็กเกจฟรีของ Supabase/YouTube ตามข้อจำกัดของแต่ละบริการ

## ส่วนที่มีในระบบ

- หน้าแอดมิน `admin-bookings.html` แยกเมนู **จัดการคอร์ส** และ **นักเรียน**
- แอดมินสร้าง/แก้ไขคอร์ส แบ่งบท เพิ่มบทเรียน ใส่ข้อความ และวาง YouTube URL
- แอดมินสร้างบัญชีนักเรียนด้วยอีเมล รหัสผ่านชั่วคราว คอร์ส และวันหมดอายุ หากอีเมลนั้นมีบัญชีอยู่แล้ว ระบบจะเพิ่มสิทธิ์คอร์สโดยใช้รหัสผ่านเดิม
- นักเรียนล็อกอินที่ `student-login.html` แล้วเรียนผ่าน `student-dashboard.html`
- ระบบจำความคืบหน้า เปลี่ยนรหัสผ่าน และระงับ/เปิดสิทธิ์รายคอร์สได้
- นักเรียนเห็นเฉพาะคอร์ส บท และบทเรียนที่เผยแพร่แล้วและตนเองมีสิทธิ์

## เปิดใช้งานครั้งแรก

1. เชื่อม Supabase CLI กับโปรเจกต์นี้ หากยังไม่ได้เชื่อม
2. ใช้ migration `supabase/migrations/20260912004803_student_learning_portal.sql` กับฐานข้อมูล
3. deploy Edge Function ด้วยคำสั่ง:

   ```bash
   supabase functions deploy create-student
   ```

4. ล็อกอินหน้าแอดมินด้วย `admin@6cat.com` หรือบัญชีที่มี `app_metadata.role` เป็น `admin`
5. เข้าเมนู **จัดการคอร์ส** แล้วทำตามลำดับ:
   - สร้างหรือเลือกคอร์ส
   - เพิ่มบท
   - เพิ่มบทเรียน
   - เปิด **เผยแพร่บทเรียน**, **เผยแพร่บท**, และเปลี่ยนสถานะคอร์สเป็น **เผยแพร่**
6. เข้าเมนู **นักเรียน** เพื่อสร้างบัญชีนักเรียนและเลือกคอร์ส
7. ส่ง URL หน้า `student-login.html` พร้อมอีเมลและรหัสผ่านชั่วคราวให้นักเรียน นักเรียนเปลี่ยนรหัสผ่านเองได้หลังเข้าสู่ระบบ

## การใส่วิดีโอแบบไม่มีค่าใช้จ่ายเพิ่ม

1. อัปโหลดวิดีโอไป YouTube แล้วตั้ง Visibility เป็น **Unlisted**
2. นำ URL หรือ Video ID มาใส่ในบทเรียน
3. Dashboard จะแสดงผ่าน `youtube-nocookie.com` และแสดงรหัสนักเรียน/อีเมลเป็นลายน้ำเคลื่อนที่

การซ่อนปุ่มดาวน์โหลด ปิดคลิกขวา และลายน้ำช่วยลดการนำไปแจกต่อ แต่เว็บเบราว์เซอร์ไม่สามารถป้องกันการจับภาพหน้าจอหรืออัดหน้าจอได้ 100% และผู้ที่ได้ลิงก์ YouTube แบบ Unlisted ยังสามารถส่งต่อลิงก์ได้ หากต้องการการป้องกันระดับ DRM จะต้องใช้บริการวิดีโอแบบเสียค่าใช้จ่ายในอนาคต

## หมายเหตุด้านข้อมูล

- การเพิ่มระบบนี้ต้องใช้ SQL migration เพราะสิทธิ์การมองเห็นถูกบังคับที่ฐานข้อมูลด้วย RLS
- ห้ามนำ Supabase service-role key มาใส่ใน HTML หรือ JavaScript ฝั่งหน้าเว็บ
- การสร้างบัญชีนักเรียนทำผ่าน Edge Function เท่านั้น เพื่อไม่เปิดสิทธิ์ Admin API ให้เบราว์เซอร์
